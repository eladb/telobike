#!/usr/bin/env node
var request = require('request');
var csv = require('csv');

var req = {
  url: 'http://telobike.citylifeapps.com/stations',
  json: true
};

request(req, function(err, res, body) {
  csv()
  console.log(body);
});

